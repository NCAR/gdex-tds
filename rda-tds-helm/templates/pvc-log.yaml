apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: {{ .Values.webapp.logPersist.fs.name }}
    namespace: {{ .Release.Namespace}}
spec:
    accessModes:
        - ReadWriteMany
    storageClassName: cephfs
    resources:
        requests:
            storage: {{ .Values.webapp.logPersist.fs.size | default "1Gi" }} 
